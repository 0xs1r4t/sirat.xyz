import styled from "styled-components";

const Json = styled.pre`
   margin: 2em;
   padding: 1em;
   border: 2px solid #000;
   border-radius: 15px;
   overflow-x: hidden;
`;

export default Json;
