# sirat.xyz

my portfolio website.

everything of importance resides in the `src` directory.

## code to text file

```bash
portfolio 22:08:32 🌼 dir_print . -I .env .git .next node_modules package-lock -O content public fonts -E code.txt
```

## clean directory structure

```bash
tree -I 'node_modules|cache|test_*|*git'
```
